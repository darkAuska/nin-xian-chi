# 《您先吃》道具系统扩展约定

版本：0.1

更新日期：2026-08-11

## 设计目标

新增道具时不改动回合结算主流程。每件道具由“静态定义 + 效果处理器”组成，通用层负责发放、选目标、输入锁定、使用次数和消耗。

## 文件职责

- `src/items.ts`：道具定义、运行时实例、注册表、发放规则、槽位与使用上限。
- `src/BanquetScene.ts`：统一选择目标流程，以及各效果处理器与画面反馈。
- `tests/items.test.ts`：不依赖 Phaser 的纯规则测试。

## 数据结构

`ItemDefinition` 是可复用的道具类型，包含：

- `id`：稳定且唯一的英文标识。
- `name`、`shortLabel`、`description`：界面文案。
- `effectId`：连接效果处理器的稳定标识。
- `foodTargetCount`：需要点击当前餐盖的次数；`0` 表示立即使用，`1` 表示对当前盆使用。固定队列模式不允许任意选择后续餐盖。
- `targetPrompt`：进入选目标状态后的提示。
- `tint`：临时图标颜色。

`ItemInstance` 是本局的一件实体道具，只保存唯一实例编号和定义编号。相同类型可以同时出现多件，并且只会消耗玩家实际点击的那一件。

## 标准生命周期

```text
每轮开始重新发放
→ 玩家点击道具
→ 通用层检查阶段与本次行动的使用上限
→ 按 foodTargetCount 收集目标
→ 通过 effectId 调用效果处理器
→ 播放反馈并消耗实例
→ 回到“决定当前盆由谁吃”的状态
```

决定谁吃、进入结算动画或 AI 回合后，道具按钮会被重绘为不可交互状态，避免重复结算。

## 新增道具步骤

1. 在 `src/items.ts` 调用 `registerItemDefinition` 增加定义。
2. 在 `BanquetScene.registerItemEffects` 中为该 `effectId` 注册处理器。
3. 处理器完成效果后消耗实例并恢复当前行动；需要延时反馈时在延时结束后恢复。
4. 在 `tests/items.test.ts` 或独立规则模块中增加效果边界测试。
5. 浏览器验证选择、取消、重复点击、换轮、换盘和重开。

## 当前与计划中的道具映射

| 道具 | 目标数 | 效果处理器 | 状态 |
|---|---:|---|---|
| 牙签 | 1 | `peek-food` | 已完成 |
| 公筷 | 0 | `swap-next-food` | 已完成 |
| 打包盒 | 1 | `remove-food` | 待实现 |
| 魔鬼辣椒油 | 0 | `boost-next-spicy` | 待实现 |

## 保持稳定的限制

- 双方各 4 个槽位。
- 第一轮 1 件、第二轮 2 件、第三轮及以后 3 件。
- 一件实例只能使用一次。
- 玩家每次行动最多使用 2 件，随后仍必须决定当前盆由谁吃。
- 首版不跨轮保存道具。
- AI 获得相同数量与槽位；AI 的公平选用逻辑在阶段四接入。
- 正常流程严格按隐藏队列顺序上菜；只有道具效果可以交换、移除或改变队列。
